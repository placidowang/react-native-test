/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './containers/App';
import {name as appName} from './containers/app.json';

AppRegistry.registerComponent(appName, () => App);
